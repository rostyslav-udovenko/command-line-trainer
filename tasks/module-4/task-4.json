{
  "id": 4,
  "description": "Use the `mount` command to list mounted filesystems.",
  "type": "mount",
  "check": {
    "expectedOutputIncludes": ["/dev/sda1", "/"]
  },
  "fs": {
    "name": "/",
    "type": "dir",
    "children": {},
    "meta": {}
  },
  "startDirectory": "/",
  "hint": "Try `mount`",
  "explanation": "The `mount` command displays all currently mounted filesystems, showing their device names and mount points. This is useful for understanding how storage devices are connected to the system and where they are accessible."
}
